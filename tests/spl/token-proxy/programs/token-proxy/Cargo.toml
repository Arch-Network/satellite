[package]
name = "token-proxy"
version = "0.1.0"
description = "Created with Anchor"
edition = "2021"

[lib]
crate-type = ["cdylib", "lib"]
name = "token_proxy"

[features]
no-entrypoint = []
cpi = ["no-entrypoint"]
idl-build = ["arch-satellite-lang/idl-build", "arch-satellite-apl/idl-build"]

[dependencies]
arch-satellite-lang = { path = "../../../../../lang" }
arch-satellite-apl = { path = "../../../../../spl" }
