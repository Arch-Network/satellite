[package]
name = "arch-satellite-lang-idl-spec"
version.workspace = true
authors = ["Arch Network <contact@arch.network>", "Anchor Maintainers <accounts@200ms.io>"]
repository = "https://github.com/Arch-Network/satellite"
edition.workspace = true
license = "Apache-2.0"
description = "Anchor framework IDL spec"

[package.metadata.docs.rs]
all-features = true
rustdoc-args = ["--cfg", "docsrs"]

[dependencies]
anyhow = "1"
serde = { version = "1", features = ["derive"] }
