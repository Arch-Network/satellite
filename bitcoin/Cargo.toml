[package]
name = "arch-satellite-bitcoin"
version.workspace = true
edition.workspace = true
description = "High-level Bitcoin utilities and re-exports for the Satellite project."
license = "MIT OR Apache-2.0"
repository = "https://github.com/Arch-Network/satellite"
readme = "README.md"
keywords = ["bitcoin", "satellite"]
categories = ["cryptography::cryptocurrencies"]

[dependencies]
arch-satellite-math = { workspace = true }
arch-satellite-collections = { workspace = true }
arch-satellite-bitcoin-transactions = { workspace = true } 
borsh = { version = "1", default-features = false }
serde = { version = "1", features = ["derive"], optional = true }
bitcoin = { version = "0.32.5" }

[features]
# Forward common features to the underlying `arch-satellite-bitcoin-transactions` crate so
# consumers can enable them directly on `arch-satellite-bitcoin`.
# We keep the same feature names for convenience.

default = [
    "arch-satellite-bitcoin-transactions/utxo-consolidation",
    "arch-satellite-bitcoin-transactions/runes",
    "arch-satellite-collections/borsh",
    "serde",
]

runes = ["arch-satellite-bitcoin-transactions/runes"]
utxo-consolidation = ["arch-satellite-bitcoin-transactions/utxo-consolidation"]
serde = ["arch-satellite-bitcoin-transactions/serde", "dep:serde"]
utoipa = ["arch-satellite-bitcoin-transactions/utoipa"] 
borsh = ["arch-satellite-collections/borsh"]